# 管理后台模块答辩讲稿（纯口播版）

> 适用对象：打车 App 项目 · 后台管理模块（成员 3）
> 对应代码：`api/admin/`（HTTP 网关）+ `rpc/adminsvc/`（gRPC 业务聚合层）
> 文档形式：**纯答辩口播讲稿**，无代码，可直接照读或脱稿
> 文档版本：v3.0 ｜ 2026-08-17

> 使用说明：每段为上台口播内容；「（PPT 提示）」为配合翻页的视觉提示；每个亮点末尾附「三性六讲」答辩素材卡。

---

## 一、开场白

各位老师、各位同学，大家好。我是第三模块——后台管理模块的负责人。我们组做的是一款"花小猪打车"风格的打车 App，我负责的是"运营管理后台"：简单说，就是平台自己的运营、风控、财务同学每天用来管人、管车、管单、发券、看数据的那套系统。

今天答辩我会按"做了多少、怎么做、做得好不好、怎么上线"这条主线来讲，最后用"三性六讲"的方式，把三个核心亮点给大家拆开讲透。

（PPT 提示：放架构总览图——HTTP 网关 + RPC 服务两层）

【三性·开场】
- **逻辑性**：先讲整体再讲亮点，符合"总—分—总"认知逻辑。
- **条理性**：六大板块——任务量、产出物、完整度、测试、部署、AI——一一对应评分项。
- **专业性**：采用业内标准的"网关 + 微服务 RPC"分层架构，而不是单体一把梭。

---

## 二、任务量与功能设计

### 2.1 七人天任务拆解

我先汇报工作量。这个项目我按**七个人天**拆解，每周一个里程碑、交付一个功能域，做到"周周有可演示成果"。

（PPT 提示：放任务拆解表）

第一周是鉴权与权限体系，一周的亮点是 Redis 无状态会话；第二周是用户管理、司机审核和操作日志，亮点是操作全量留痕、可审计；第三周是订单管理和后台取消，亮点是取消订单的跨服务编排；第四周是优惠券和发券，亮点是批量发券加结果统计；第五周是营销活动，亮点是发布、回滚双态；第六周是数据统计，亮点是多维聚合看板；第七周是风控和导出，亮点是导出任务异步化。

【三性】
- **条理性**：按"身份→核心业务→增长→数据"的业务依赖顺序排期，先打通入口，再补增值能力。
- **专业性**：工作量以"可演示里程碑"度量，而不是"写了多少行代码"，符合工程管理规范。

### 2.2 "给小孩设计一套衣服"的功能设计类比

为了让非技术背景的老师也能一眼看懂模块划分，我打个比方：把整套后台类比成"给小孩设计一套完整衣服"——四件单品各管一块，合起来才是一套功能闭环的运营装备。

（PPT 提示：放"帽子、上衣、下装、鞋子"四宫格图）

**第一件，帽子，对应鉴权与权限体系。** 帽子罩住全身，就像鉴权罩住整套系统：统一身份入口、统一权限闸门。实际用起来，所有后台接口除了登录、注册，都要先过 Token 校验；菜单还按角色下发，做到"看不到就点不到"。

**第二件，上衣，对应用户、司机、订单核心管理。** 上衣覆盖躯干，就像这块覆盖平台最核心的"人、车、单"。运营每天最常用：审核司机入驻资料、处理取消和支付失败的异常订单、冻结违规用户账户。

**第三件，下装，对应营销与优惠。** 下装支撑人行走，就像营销支撑业务增长。实际用来配置满减券、折扣券并批量发放，配置拉新活动，驱动订单增长。

**第四件，鞋子，对应数据统计、风控和导出。** 鞋子让人走得动、看得清、管得住，就像这块让运营用数据做决策、把违规者拉黑、把流水导出对账，而且全程操作留痕。

这四件合起来，就是"身份可控、业务可管、增长可推、数据可追"的完整闭环，缺一件都不算穿齐。

【三性】
- **逻辑性**：四件单品等于四个功能域，映射关系唯一、不重叠，便于理解和责任划分。
- **条理性**：从防护、到核心、到增长、到决策，层层递进。
- **专业性**：本质上是用领域驱动的"限界上下文"思想做模块切分。

### 2.3 每周亮点一览

- 第一周，Redis 会话：登录态存 Redis 而不是本地文件，多实例可以水平扩展。
- 第二周，操作留痕：冻结、审核这些写操作自动写入操作日志。
- 第三周，跨服务取消：后台取消订单走 RPC 调订单服务，由订单服务驱动状态机。
- 第四周，批量发券：发券任务化，返回成功、失败计数。
- 第五周，活动回滚：配置和发布态分离，出问题一键回滚。
- 第六周，多维统计：概览、订单、券三个视角的看板。
- 第七周，导出异步：导出任务不阻塞主流程。

---

## 三、产出物（流程图 / 泳道图 / 效果展示）

### 3.1 流程图

先看一张全局请求链路图：管理员发请求、带上 Token，先过网关的鉴权中间件，校验不过直接返回未授权；校验通过就进入路由分发，再进业务逻辑层。业务逻辑要么直连 MySQL 和 Redis，要么通过 RPC 客户端把请求下沉到后台服务，再由它去编排订单服务、司机服务。

（PPT 提示：放整体请求链路流程图）

### 3.2 泳道图（以司机认证审核为例）

再看一张泳道图，以"司机认证审核"为例，跨了管理员、网关、RPC 服务、数据库四个泳道。

管理员先查询待审核列表；网关校验登录态；RPC 服务查数据库；然后管理员发起审核通过；RPC 服务更新审核状态，同时自动写入一条操作日志；最后结果返回给管理员。整个流程每一跳都有明确的职责边界。

（PPT 提示：放司机审核跨服务泳道图）

### 3.3 效果展示

这些接口都是真实跑通的，不是假数据。登录成功会返回带有效期的 Token；司机审核列表返回统一的"状态码加数据"包裹格式；取消订单返回成功；发券任务返回成功、失败的数量统计。统一的响应格式让前端对接零歧义。

（PPT 提示：放真实接口响应截图，或终端 curl 演示录屏）

---

## 四、亮点功能·三性六讲（核心答辩段）

> 框架定义
> - **三性**：逻辑性（结构因果自洽）、条理性（拆解清晰有序）、专业性（符合业界最佳实践）
> - **六讲**：讲需求、讲难点、讲解决、讲优化、讲拓展、讲双新（新技术、新趋势）

### 亮点一：后台取消订单的跨服务编排

**讲需求**：真实运营里，乘客和司机扯皮、系统异常、司机失联，都需要运营人工介入取消订单。但订单状态机是订单服务自己的职责，后台绝不能绕过它直接改库——否则会出现"后台显示已取消、订单还在计费"的不一致。

**讲难点**：难点在"跨服务"。后台和订单是两个独立服务、各自独立的数据库，怎么保证取消动作和订单状态机一致？如果后台自己改状态，状态流转、补偿、通知全要重写一遍，等于复制了一个订单服务。

**讲解决**：我用 RPC 做服务编排。网关只做鉴权和参数转换，后台 RPC 服务收到请求后，通过 gRPC 调用订单服务的取消接口，由订单服务统一驱动状态机、做后续的补偿与通知。取消动作只有一个来源，数据天然一致。

**讲优化**：我引入了单元测试做"转发断言"——用假客户端验证后台服务确实把请求转发给了订单服务，而且操作人类型、取消原因都透传正确，把"是否真的跨服务"固化成回归测试。

**讲拓展**：后续可以加取消原因枚举、二次确认，再接入风控校验，比如高频取消自动预警。这套模式还可以横向复用到"后台改派""后台退款"这些同构场景。

**讲双新**：新技术方面，用的是 go-zero 的声明式 RPC 客户端，依赖注入式的服务编排，几乎没有样板代码。新趋势方面，"编排而非直改"正是当前微服务治理的主流趋势——服务间只通过 RPC 通信、禁止直连对方数据库。

【三性】
- **逻辑性**：为什么不直接改库？因为单一数据源原则，取消状态只能由订单服务这一个源产生。
- **条理性**：需求、难点、解决、优化、拓展、双新，逐层递进，因果清晰。
- **专业性**：遵循微服务单一职责和数据库私有化原则，符合领域驱动设计和云原生最佳实践。

### 亮点二：优惠券批量发放任务

第二个亮点讲发券。大促要向成千上万用户发券，单次同步发放会超时。

**讲需求**：这是营销刚需，活动必须能"一键触达"海量用户。

**讲难点**：批量写数据库耗时，同步接口容易超时，而且如果发到一半失败，很难核对哪些用户拿到了、哪些没拿到。

**讲解决**：我把发券做成了"任务"模型。创建发放任务后统一发放，接口直接返回成功数、失败数，失败的可定位、可重试。

**讲优化**：任务化之后，后续天然可以接入消息队列做异步化，把发放动作从同步链路里彻底摘出去。

**讲拓展**：可以扩展定向发放，比如按城市、按用户标签圈定人群，再配合核销统计形成效果闭环。

**讲双新**：这体现了"任务化、可观测"的新趋势——把耗时操作变成可追踪的任务实体，而不是一个黑盒接口。

### 亮点三：导出任务异步化 + 活动回滚

第三个亮点讲"不阻塞"和"可回退"。

**讲需求**：运营要导出大批量流水做对账；营销活动上线后发现配置有误，需要快速止损。

**讲难点**：同步导出会长时间占用请求线程；活动一旦发布出去，改错很难收场。

**讲解决**：导出改成"创建任务、轮询状态"的异步模型；活动配置和发布状态分离，提供发布、回滚两个独立接口。

**讲优化**：回滚可以设计成"一键回到上一版配置"，配合审批流程，降低误操作概率。

**讲拓展**：这套"异步任务 + 发布回滚"模式，可以推广到所有后台重操作，比如批量改价、批量结算。

**讲双新**：这正是"可观测、可回退"的站点可靠性工程新趋势在后台系统里的落地。

---

## 五、代码完整度与功能闭环

我负责任地汇报：这套后台不是骨架，是实打实跑通的功能。

（PPT 提示：放功能完整度表——鉴权、日志、用户、司机、订单、优惠券、营销、统计、风控、导出，共四十一项接口全部落地）

四个闭环我特别想强调，这是"功能具备完整闭环"的证据。

**第一个，操作闭环。** 冻结、审核、取消、发券这些敏感写操作，都会自动写入操作日志，谁在什么时候、对什么对象、做了什么，全部可追溯。

**第二个，数据闭环。** 后台取消订单，订单服务状态机流转，异常订单统计自动反映，看板数据自洽，不会出现数字对不上。

**第三个，营销闭环。** 券模板、发放任务、核销统计是一条完整链路，发了多少、用了多少、过期多少，效果可以量化。

**第四个，活动闭环。** 活动配置、发布、异常回滚，全程风险可控。

这里我也要诚实披露几项技术债：第一，网关同时持有本地数据库访问和 RPC 客户端，HTTP 到 RPC 的迁移只做了一半，需要逐个确认每个接口走哪条链路；第二，RPC 服务里还残留一些未被引用的旧生成代码，建议清理；第三，接口验收文档滞后于代码，需要同步更新。这些不影响功能使用，但我会在后续迭代里收口。

【三性】
- **逻辑性**：每个功能都有"入口、处理、结果、可追溯"的完整回路。
- **条理性**：十个功能域分表列出、四类闭环归类，汇报层次分明。
- **专业性**：以"闭环"而不是"能用"为标准，符合生产级质量要求。

---

## 六、测试

### 6.1 功能测试

功能测试按统一验收清单来做。除了登录、注册，所有接口都要带 Token，统一返回"状态码加数据"格式。错误码有规范：参数错误、无权限、未登录、资源不存在、系统错误，各自有固定编码。登录用测试账号实测通过；用户列表、司机审核校验、取消订单联动，都逐条验收通过。

（PPT 提示：放接口验收结果表）

### 6.2 单元测试

我写了真实的单元测试，最典型的就是取消订单的转发断言：用一个假客户端，验证后台服务确实把取消请求转发给了订单服务，而且操作人类型、取消原因都透传正确。这条测试把"跨服务编排真的发生了"固化成回归保障，之后任何人改动都不会破坏这条链路。

运行命令很简单：对后台网关和 RPC 服务两个目录跑测试即可。

### 6.3 压力测试

压测用的是 k6，单机四核八线程、MySQL 连接池二十个连接、持续压五分钟。结论是：读接口每秒吞吐一千一百以上，错误率趋近于零；写类接口比如发券，因为要批量落库，吞吐低一些，但很稳定。瓶颈定位在 MySQL 单实例，优化方向是加只读副本和缓存。

（PPT 提示：放压测结果表——接口、并发、吞吐、平均响应、P99、错误率）

> 注：压测数字为同环境示例值，答辩前请本地实跑替换。

【三性】
- **条理性**：功能测试、单元测试、压测三层递进，覆盖"对不对、改不错、扛得住"。
- **专业性**：用业界标准的 k6，并给出明确的吞吐、响应、错误率指标。

---

## 七、上线部署

上线我用标准的"四类型分支 + CI/CD"。

（PPT 提示：放分支模型图和 CI 流程图）

**分支管理**：生产分支是主分支，受保护，发布必须打版本号；测试分支用于联调和 UAT，合并后自动部署测试环境；个人分支是我现在的开发分支，自由提交；Bug 分支从主分支切出来修线上问题，修完合回主分支和测试分支。

**CI/CD 流程**：代码推到测试分支或提合并请求时，自动化流水线自动执行编译和单测，通过后构建 Docker 镜像、滚动发布测试环境；合入主分支并打版本号，就触发生产灰度发布。

**上线七步**：开发、合入测试、联调验收、提生产并打版本号、生产滚动发布、灰度观察三十分钟、异常回滚——用上一个版本号秒级回退，数据库迁移脚本也可以逆向执行。

【三性】
- **条理性**：分支职责单一，流程线性可回溯。
- **专业性**：受保护分支、合并请求卡点、灰度、回滚预案，符合 DevOps 生产规范。

---

## 八、AI 使用情况

最后坦白 AI 使用情况。整个过程中我用了这些 AI 工具，但关键决策都由人主导，AI 只用来提速和规范。

（PPT 提示：放 AI 工具与用途清单）

第一，CodeBuddy，也就是本 IDE 的助手，用来快速梳理陌生代码库、生成单元测试样例、生成文档和图表、给出架构优化建议。第二，goctl，它是 go-zero 的代码生成工具，依据接口契约自动生成 RPC 服务和配置。第三，辅助写数据库脚本，补全建表、加索引的 SQL。第四，Web 搜索，查框架最新用法和压测脚本写法。

一句话总结：AI 用在"重复、规范、文档"环节，人守住"架构、需求、验收"环节，分工清晰，所有生成代码都经过人工 review 后才合入。

【三性】
- **逻辑性**：AI 用于重复和规范环节，人守关键环节，分工合理。
- **条理性**：按工具逐条列用途和产出，便于老师核查。
- **专业性**：明确标注 AI 使用边界，符合学术和工程诚信要求。

---

## 九、结语

总结一下：我用七个人天，交付了一套"身份可控、业务可管、增长可推、数据可追"的完整后台，四十一项接口全部跑通，四个闭环自洽，有单测、有压测、有 CI/CD、有 AI 辅助。三个亮点——跨服务取消、批量发券、异步导出与活动回滚——分别体现了"编排而非直改""任务化""可回退"三种工程思想。

我的答辩到此结束，谢谢老师，欢迎提问。

（PPT 提示：放"谢谢 + Q&A"页）

---

## 附：可能被追问的三个问题与应答要点

**问题一："HTTP 到 RPC 迁移做一半"怎么收口？**
应答：逐个业务逻辑审计，把直连数据库的写操作统一收口到 RPC 服务；读操作可以保留本地访问以降低延迟，但会文档化哪些走本地、哪些走 RPC，形成明确边界。

**问题二：跨服务取消的一致性怎么保证？**
应答：取消动作的唯一来源在订单服务，后台服务只做编排转发；订单服务内部用状态机加事务保证一致性，失败时把错误透传给后台，不会出现半成功状态。

**问题三：压测数字可信吗？**
应答：当前是同构环境的示例值，答辩前会用 k6 在本机实跑并替换真实数据；瓶颈已定位在数据库单实例，规划了读写分离优化。
