# 管理后台订单与司机审核当日联调记录

> 联调日期：2026-08-17  
> 联调范围：后台订单列表/详情、司机资质审核通过/驳回  
> 目标：运营后台能查到 `ordersvc` 创建的真实订单；能对 mock 司机资质发起审核通过或驳回。

## 一、联调链路

| 能力 | HTTP 路由 | 后台链路 | 下游依赖 |
| --- | --- | --- | --- |
| 订单列表 | `GET /admin/v1/orders` | `api/admin -> adminsvc.ListOrders -> ride_order` | `ordersvc` 创建订单数据 |
| 订单详情 | `GET /admin/v1/orders/{id}` | `api/admin -> adminsvc.GetOrder -> ride_order/order_status_log` | `ordersvc` 创建订单和状态日志 |
| 司机审核列表 | `GET /admin/v1/driver-certifications` | `api/admin -> adminsvc.ListDriverCertifications -> driver_certification` | mock 司机认证数据 |
| 司机审核通过 | `POST /admin/v1/driver-certifications/{id}/approve` | `api/admin -> adminsvc.ApproveDriverCertification -> driversvc.ApproveCertification` | `driversvc` |
| 司机审核驳回 | `POST /admin/v1/driver-certifications/{id}/reject` | `api/admin -> adminsvc.RejectDriverCertification -> driversvc.RejectCertification` | `driversvc` |

## 二、服务配置

| 服务 | 临时监听地址 | 说明 |
| --- | --- | --- |
| `ordersvc` | `127.0.0.1:50051` | 使用 `.gotmp/ordersvc-test.yaml`，连接本地 `xiaolong_ridy` |
| `driversvc` | `127.0.0.1:18081` | 使用 `.gotmp/driversvc-test.yaml`，避免与 adminsvc 端口冲突 |
| `adminsvc` | `127.0.0.1:8080` | 使用 `.gotmp/adminsvc-test.yaml`，`DriversRPC.Target=127.0.0.1:18081` |
| `api/admin` | `127.0.0.1:8888` | 使用 `api/admin/etc/admin.json` |

## 三、验收数据

### 3.1 ordersvc 创建订单

通过 `ordersvc.CreateOrder` 创建订单，确认订单写入 `ride_order`，状态日志写入 `order_status_log`。

| 字段 | 值 |
| --- | --- |
| `order_id` | `2` |
| `order_no` | `DD202608171457049761` |
| `user_id` | `880001` |
| `from_address` | `后台验收起点` |
| `to_address` | `后台验收终点` |
| `estimated_price` | `23.50` |
| `status` | `1`，待接单 |

### 3.2 mock 司机认证数据

| 用途 | `certification_id` | `driver_id` | `vehicle_id` | 初始审核状态 |
| --- | --- | --- | --- | --- |
| 审核通过 | `3` | `3` | `3` | `1`，待审核 |
| 审核驳回 | `4` | `4` | `4` | `1`，待审核 |

## 四、HTTP 验收结果

| 接口 | 请求 | HTTP 状态 | 业务码 | 关键结果 |
| --- | --- | --- | --- | --- |
| 订单列表 | `GET /admin/v1/orders?keyword=DD202608171457049761&page=1&page_size=5` | `200` | `0` | 返回 `total=1`，订单号匹配 |
| 订单详情 | `GET /admin/v1/orders/2` | `200` | `0` | 返回订单主信息和 `status_logs[0].remark=创建订单` |
| 审核列表 | `GET /admin/v1/driver-certifications?audit_status=1&page=1&page_size=10` | `200` | `0` | 返回两条待审核 mock 认证 |
| 审核通过 | `POST /admin/v1/driver-certifications/3/approve` | `200` | `0` | 返回 `message=ok` |
| 审核驳回 | `POST /admin/v1/driver-certifications/4/reject` | `200` | `0` | 返回 `message=ok` |
| 通过后详情 | `GET /admin/v1/driver-certifications/3` | `200` | `0` | `audit_status=2`、`driver_status=2`、`vehicle_status=2` |
| 驳回后详情 | `GET /admin/v1/driver-certifications/4` | `200` | `0` | `audit_status=3`，驳回原因为 `当日验收：资料需补充` |

## 五、数据库状态确认

| `certification_id` | `driver_status` | `vehicle_status` | `audit_status` | `audit_remark` | `audited_by` |
| --- | --- | --- | --- | --- | --- |
| `3` | `2` | `2` | `2` | `当日验收：审核通过` | `1` |
| `4` | `1` | `1` | `3` | `当日验收：资料需补充` | `1` |

## 六、结论

本次当日验收通过：

1. 后台订单列表和详情能查到 `ordersvc` 创建的真实订单。
2. 后台司机审核通过和驳回均已通过 `adminsvc -> driversvc` 完成。
3. 审核通过后司机和车辆状态已联动为正常；审核驳回后认证状态为驳回并保留驳回原因。
