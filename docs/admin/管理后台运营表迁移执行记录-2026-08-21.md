# 管理后台运营表迁移执行记录

## 执行范围

远程数据库 `xiaolong_ridy`，执行脚本：`scripts/sql/migrate/07_admin_operation_business.sql`。

## 执行方式

先查询 `information_schema.tables`，仅对缺失表执行脚本中对应的 `CREATE TABLE IF NOT EXISTS` 语句。未执行任何 `INSERT`、`UPDATE`、`DELETE` 或已有表结构变更。

## 执行结果

以下六张表均已创建并完成索引数量核验：

- `admin_complaint_work_order`：7 个索引。
- `admin_work_order_flow`：4 个索引。
- `admin_work_order_evidence`：3 个索引。
- `admin_coupon_publish_record`：3 个索引。
- `admin_coupon_issue_task`：4 个索引。
- `risk_blacklist_hit_record`：4 个索引。

## 后续迁移

`admin_export_task` 与 `admin_audit_outbox` 位于 `scripts/sql/migrate/09_admin_export_audit_task.sql`，不属于本次获授权执行的 `07` 脚本，待单独授权后执行。
