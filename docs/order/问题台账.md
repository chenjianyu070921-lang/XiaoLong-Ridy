# 订单与派单调度 问题台账

> 范围：rpc/ordersvc、rpc/dispatchsvc、mq-consumer/order-event-consumer、job（超时取消部分）。
> 状态说明：待处理 / 处理中 / 已解决 / 已关闭（解决后 2 天无回归可关闭）。

## 当前问题清单

| # | 优先级 | 问题 | 影响 | 建议方案 | 负责人 | 状态 | 解决日期 | 每日进展 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | P0 | 取消与接单并发竞态：Cancel/TimeoutCancel 不抢接单锁，且 DB 条件允许 status IN (1,2)，司机接单后仍可被乘客/系统取消 | 司机端出现“已接单→已取消”跳变，无违约金、无派单记录回写 | 取消/超时与接单共用同一把 Redis 订单锁；取消 UPDATE 条件改为“当前状态=读取状态”；接单成功后取消需走违约金分支 | 成员4 | 待处理 | - | - |
| 2 | P0 | ConfirmPaid 无支付校验：不查 payment 表，不校验 paymentNo/金额/paid_at，内部调用方可直接完成未支付订单 | 订单可被伪造为已完成，资金与状态不一致 | 由 paysvc 回调触发，携带支付单号并校验支付单状态与金额；订单落实际应付金额用于比对 | 成员5+成员4 | 待处理 | - | - |
| 3 | P0 | FinishTrip 支付单创建失败无补偿：订单置待支付后调 paysvc 失败仅记日志，无 payment 记录、无重试、无支付超时关单 | 订单永久卡在待支付，乘客无法支付 | 支付失败进入补偿队列/状态；新增支付超时关单任务；幂等重试创建支付单 | 成员5+成员4 | 待处理 | - | - |
| 4 | P0 | 派单不幂等：DispatchOrder 每次无条件插入候选记录，无 (order_id,driver_id) 唯一键；事件+同步直派可能双触发 | 重复派单记录，司机重复收到单，统计错乱 | dispatch_record 加唯一键；派单前先查重；事件与直派二选一，统一走事件+补偿 | 成员4 | 待处理 | - | - |
| 5 | P1 | GEO 无司机时自动回退 mock 假司机（9001/9002/9003）并写派单记录 | 线上无司机也显示派单成功，无法区分真实可用 | mock 回退仅联调配置开启；生产无候选返回“无司机”并触发改派/通知 | 成员4 | 待处理 | - | - |
| 6 | P1 | 接单不校验司机资格：任意 driver_id 可接任意订单，不校验候选列表/在线/空闲/已有进行中订单 | 司机可乱接单、同时接多单 | 接单前校验 dispatch_record 候选记录、司机在线状态与 busy 标记；司机侧加唯一约束 | 成员2+成员4 | 待处理 | - | - |
| 7 | P1 | 事件消费者静默 ACK 非 order.created 主题；取消/到达/开始/结束未发布统一事件 | 后续扩展推送/通知会丢事件 | 消费者按 topic 分发，未知 topic 记日志并落死信；状态变更统一发布 order.status.changed | 成员4+成员6 | 待处理 | - | - |
| 8 | P1 | 超时任务无分布式锁，多实例重复取消；TimeoutCancel 允许已接单但 ListTimeoutOrders 只查待接单，口径不一致 | 多实例重复取消，接单后无“超时未出发”兜底 | job 加 Redis 分布式锁；统一超时语义（待接单超时/已接单超时两套） | 成员4+成员6 | 待处理 | - | - |
| 9 | P1 | 结算重算固定北京城市：settleAmountCents 写死 defaultCityCode，city_code 未随订单落库 | 非北京订单按北京规则重算，10% 偏差可能误拒 | ride_order 增加 city_code 字段，创建订单落库；结算按订单城市计价 | 成员4+成员5 | 待处理 | - | - |
| 10 | P1 | 创建订单无幂等键与业务约束：无 X-Idempotency-Key、无乘客未完成订单校验、无用户存在校验 | 重复点击生成多单 | 网关/ordersvc 支持幂等键；创建前校验乘客状态与未完成订单 | 成员1+成员4 | 待处理 | - | - |
| 11 | P2 | 行程实际数据不落库：实际里程/时长/费用只写状态日志 Remark，订单表无 actual 字段，无轨迹 | 无法对账、无法回放行程 | ride_order 增加 actual_distance/duration/amount；轨迹点接入 location 模块 | 成员4+成员6 | 待处理 | - | - |
| 12 | P2 | dispatch_record 状态机不完整：只有 MarkDispatchAccepted，拒绝/超时/改派状态无接口 | 派单过程无法完整留痕 | 补拒绝/超时/改派状态流转与事件 | 成员4 | 待处理 | - | - |

## 每日更新规则

- 每天 18:00 复盘会更新本表：已完成项把“状态”改为“已解决”并填解决日期与进展。
- P0 未解决前，当日联调报告必须说明阻塞点。
- 新增问题追加到表尾，编号继续递增。
