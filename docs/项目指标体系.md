# 网约车司机端项目指标体系

版本：P0 v1.0
日期：2026-08-20

## 1. 功能完成指标

| 指标 | 目标 | 验证方式 |
|------|------|----------|
| 订单主链路接口完成率 | 5/5 | 接口契约、路由和单元测试 |
| 拒单 RPC 接通率 | 100% | `RejectDispatch` 调用成功 |
| 我的派单列表组合率 | 100% | dispatchsvc + ordersvc.GetOrder |
| 真实数据闭环 | 至少 1 条 | API 响应与 MySQL 查询对照 |
| 参数校验覆盖 | 非法 ID、分页、结束行程数据 | logic 单测 |

## 2. 数据正确性指标

| 指标 | 目标 |
|------|------|
| 拒单状态一致性 | 接口返回状态与 `dispatch_record.status` 一致 |
| 司机归属隔离 | 查询只返回 JWT 司机自己的派单记录 |
| 订单详情一致性 | 列表订单摘要与 `ride_order` 数据一致 |
| 关键字段完整率 | order_id、driver_id、status、created_at 不为空 |

## 3. 稳定性指标

| 指标 | P0 目标 |
|------|---------|
| 非法请求拦截 | 100% 在 API/logic 层拦截 |
| 下游不可用反馈 | 返回明确 502，不返回伪成功 |
| 分页上限 | pageSize 最大 100 |
| 链路可追踪 | 每次 HTTP 响应返回 traceId |

## 4. 真实联调指标

2026-08-20 已完成一次真实数据闭环：

- 司机 `30` 成功注册并写入 MySQL。
- 订单 `5` 与派单记录 `4` 被司机列表接口查询。
- 拒单后派单记录 `4` 的状态为 `3`，数据库与接口响应一致。

## 5. 后续迭代指标

P1：补充司机在线状态、位置上报、超时重派和事件监控指标。

P2：补充收入统计、历史订单、评价和异常订单处理，不纳入当前 P0 验收。