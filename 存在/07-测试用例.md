# 司机端测试用例

> 覆盖正向 / 反向 / 异常。实际结果栏按实训自测填写，"—"表示未在答辩环境实测、需补测。

## 1. 上线 / 心跳 / 互踢

| 用例ID | 测试场景 | 输入 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| TC-ON-01 | 正常上线 | driverId合法, deviceId="A", lon=116.4, lat=39.9 | online_status=1, Redis写在线+TTL | — |
| TC-ON-02 | 上线driverId不合法 | driverId=0 | 返回错误, 不写在线 | — |
| TC-ON-03 | 上线经纬度非法 | lon=999, lat=999 | 返回参数错误 | — |
| TC-HB-01 | 正常心跳保活 | deviceId="A" 与上线一致 | kicked=false, TTL续期 | — |
| TC-HB-02 | 多端互踢 | 先用A上线, 再用B心跳同driverId | B返回kicked=false(A被顶), A再次心跳返回kicked=true | — |
| TC-HB-03 | 心跳deviceId为空 | deviceId="" | 返回"设备标识缺失"错误 | — |
| TC-HB-04 | Redis不可用 | 模拟Redis宕机 | Heartbeat返回err, 客户端提示重连(未做重试) | — |
| TC-OFF-01 | 正常下线 | driverId合法 | online_status=0, Redis置离线 | — |

## 2. 接单 / 行程 / 完单

| 用例ID | 测试场景 | 输入 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| TC-ACC-01 | 正常接单 | driverId>0, orderId>0 | ordersvc接单成功, 返回status | — |
| TC-ACC-02 | 接单参数非法 | driverId=0 或 orderId=0 | 返回ErrInvalidParam | — |
| TC-ACC-03 | ordersvc异常 | ordersvc宕机 | 返回RPC错误, 不写行程状态 | — |
| TC-STR-01 | 开始行程 | orderId合法 | ordersvc StartTrip成功 + driversvc置行程中(2) | — |
| TC-FIN-01 | 正常完单 | 里程/时长/金额均>=0 | 返回应付金额 + driversvc置回在线(1) | — |
| TC-FIN-02 | 完单负数参数 | actualDistanceM=-1 | 返回ErrInvalidParam, 不调ordersvc | — |
| TC-ARR-01 | 确认到达 | orderId合法 | ordersvc ConfirmArrive成功 | — |

## 3. 派单列表分页

| 用例ID | 测试场景 | 输入 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| TC-DSP-01 | 正常分页 | driverId>0, page=1, pageSize=10, status=1 | 返回<=10条 + total + page | — |
| TC-DSP-02 | 越界分页 | page=0 / pageSize=0 | clampPage修正为默认页 | — |
| TC-DSP-03 | driverId非法 | driverId=0 | 返回ErrInvalidParam | — |
| TC-DSP-04 | dispatchsvc异常 | dispatchsvc宕机 | 返回RPC错误 | — |
| TC-DSP-05 | 订单拼装失败 | 某dispatch的orderId在ordersvc查不到 | 返回错误(当前实现逐条查, 一条失败整体失败) | — |

## 4. AI 推荐分

| 用例ID | 测试场景 | 输入 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| TC-AI-01 | 正常有服务分 | driverId有score记录 | 返回0~100分 + factors + degraded=false | — |
| TC-AI-02 | 无服务分记录 | driverId无score | degraded=true, 回退距离优先, 不报错 | — |
| TC-AI-03 | 服务分查询异常 | 模拟DB错误 | degraded=true + degradeReason | — |
| TC-AI-04 | driverId非法 | driverId=0 | 返回"司机ID不合法" | — |

## 5. 资质

| 用例ID | 测试场景 | 输入 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| TC-CERT-01 | 上传资质 | driverId/vehicleId/四张图URL | 写入资质记录, auditStatus=待审核 | — |
| TC-CERT-02 | 查询资质存在 | driverId有记录 | found=true + certification | — |
| TC-CERT-03 | 查询资质不存在 | driverId无记录 | found=false | — |
