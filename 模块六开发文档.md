# 模块六：位置与基础设施 — 开发文档

> 成员 6 | `rpc/locationsvc` + `rpc/pushesvc` + `mq-consumer/location-consumer` + `job` + `common`

---

## 一、环境信息

| 项目 | 值 |
|------|-----|
| Go 版本 | 1.25.0 |
| 框架 | go-zero v1.7.2 |
| 项目路径 | `D:\gowork\XiaoLong-Ridy` |
| Module 名 | `XiaoLong-Ridy` |

### 依赖中间件

| 服务 | 端口 | 用户名 | 密码 | Docker 容器名 |
|------|------|--------|------|---------------|
| etcd | 2379 | - | - | etcd |
| MySQL | 3306 | root | root | mysql-local |
| Redis | 6379 | - | 无 | redis-local |
| Kafka | 9092 | - | - | 待部署 |

---

## 二、新增/修改文件清单（39个文件）

### 修改的文件（2个）

| 文件 | 说明 |
|------|------|
| `go.mod` | 改 go 版本为 1.25.0，加入 go-zero/grpc/protobuf 依赖 |
| `go.sum` | `go mod tidy` 自动生成 |

### common/ — 公共包（5个）

| 文件 | 说明 |
|------|------|
| `common/errorx/baseerror.go` | 统一错误类型 `CodeError`，gRPC 错误转换 `GrpcError()` |
| `common/errorx/errormsg.go` | 错误码常量（OK=0, ParamError=400, ServerError=500...），错误消息映射 |
| `common/response/response.go` | 统一返回格式 `{code, msg, data}`，`Success()`、`Error()`、`ParamError()` |
| `common/constants/constants.go` | 订单/司机/支付状态常量，Redis Key 前缀，Kafka Topic 名称 |
| `common/keyutil/keyutil.go` | ID 生成器（`GenerateOrderID`、`GenerateDriverID`、`GenerateUserID`） |

### rpc/locationsvc/ — 位置服务（13个）

| 文件 | 说明 |
|------|------|
| `rpc/locationsvc/locationsvc.proto` | Proto 定义：ReverseGeocode、POISearch、RoutePlan 三个 RPC 接口 |
| `rpc/locationsvc/locationsvc.pb.go` | protoc 自动生成的 message 结构体 |
| `rpc/locationsvc/locationsvc_grpc.pb.go` | protoc 自动生成的 gRPC Service 接口 |
| `rpc/locationsvc/locationsvc.go` | main 入口，启动 RPC 服务（dev/test 模式开启 gRPC reflection，便于调试） |
| `rpc/locationsvc/etc/locationsvc.yaml` | 配置文件，端口 9001，含高德地图 API 配置（ApiKey / BaseUrl） |
| `rpc/locationsvc/internal/config/config.go` | 配置结构体（含 `MapServiceConfig`） |
| `rpc/locationsvc/internal/svc/servicecontext.go` | 依赖注入容器（注入 MySQL、高德客户端、POI 缓存模型，启动时自动建 `poi` 表） |
| `rpc/locationsvc/internal/server/locationsvcserver.go` | gRPC Server 实现（转发到三个 logic） |
| `rpc/locationsvc/internal/logic/reversegcodelogic.go` | 逆地址解析逻辑（经纬度→地址），调高德 regeo 接口 |
| `rpc/locationsvc/internal/logic/poisearchlogic.go` | POI 搜索逻辑，先查本地缓存再调高德，结果写回缓存 |
| `rpc/locationsvc/internal/logic/routeplanlogic.go` | 路径规划逻辑（Haversine 球面距离公式，暂为估算值） |
| `rpc/locationsvc/internal/geo/amap.go` | 高德地图 HTTP 客户端（POI 搜索 / 逆地理编码） |
| `rpc/locationsvc/internal/model/poi.go` | POI 缓存表模型（`poi` 表）+ 数据访问（SearchByName / BatchInsert） |

### rpc/pushesvc/ — 推送服务（11个）

| 文件 | 说明 |
|------|------|
| `rpc/pushesvc/pushesvc.proto` | Proto 定义：SendNotice、SendPush、SendSMS 三个 RPC 接口 |
| `rpc/pushesvc/pushesvc.pb.go` | protoc 自动生成 |
| `rpc/pushesvc/pushesvc_grpc.pb.go` | protoc 自动生成 |
| `rpc/pushesvc/pushesvc.go` | main 入口，启动 RPC 服务 |
| `rpc/pushesvc/etc/pushesvc.yaml` | 配置文件，端口 9002，含短信/推送/MySQL 配置 |
| `rpc/pushesvc/internal/config/config.go` | 配置结构体 |
| `rpc/pushesvc/internal/svc/servicecontext.go` | 依赖注入容器 |
| `rpc/pushesvc/internal/server/pushsvcserver.go` | gRPC Server 实现 |
| `rpc/pushesvc/internal/logic/sendnoticelogic.go` | 发送站内信逻辑 |
| `rpc/pushesvc/internal/logic/sendpushlogic.go` | App 推送逻辑 |
| `rpc/pushesvc/internal/logic/sendsmslogic.go` | 短信发送逻辑 |

### mq-consumer/location-consumer/ — 位置消息消费者（4个）

| 文件 | 说明 |
|------|------|
| `mq-consumer/location-consumer/locationconsumer.go` | main 入口 |
| `mq-consumer/location-consumer/etc/location-consumer.yaml` | Kafka / Redis 配置 |
| `mq-consumer/location-consumer/internal/config/config.go` | 配置结构体 |
| `mq-consumer/location-consumer/internal/handler/locationhandler.go` | 位置消息处理器，消费司机位置上报 |

### job/ — 定时任务（4个）

| 文件 | 说明 |
|------|------|
| `job/job.go` | main 入口（3 个定时器：每小时清理/每10分钟同步/每日报表） |
| `job/etc/job.yaml` | MySQL / Redis 配置 |
| `job/internal/config/config.go` | 配置结构体 |
| `job/internal/handler/cleanuphandler.go` | 定时任务处理器（CleanExpiredLocation、SyncOrderStatus、DailyReport） |

---

## 三、数据库建表 SQL

### 3.1 notices — 站内信表（必须）

```sql
CREATE TABLE IF NOT EXISTS notices (
    id         BIGINT        NOT NULL AUTO_INCREMENT  COMMENT '主键ID',
    user_id    BIGINT        NOT NULL                 COMMENT '用户ID',
    title      VARCHAR(200)  NOT NULL                 COMMENT '标题',
    content    TEXT          NOT NULL                 COMMENT '内容',
    biz_type   TINYINT       NOT NULL DEFAULT 1       COMMENT '业务类型: 1=订单通知 2=系统通知 3=活动通知',
    is_read    TINYINT       NOT NULL DEFAULT 0       COMMENT '是否已读: 0=未读 1=已读',
    created_at DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    INDEX idx_user_id (user_id),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='站内信表';
```

### 3.2 driver_location — 司机实时位置表（模块四使用）

```sql
CREATE TABLE IF NOT EXISTS driver_location (
    id          BIGINT   NOT NULL AUTO_INCREMENT COMMENT '主键',
    driver_id   BIGINT   NOT NULL                COMMENT '司机ID',
    lat         DOUBLE   NOT NULL                COMMENT '纬度',
    lng         DOUBLE   NOT NULL                COMMENT '经度',
    speed       DOUBLE   DEFAULT 0               COMMENT '速度 km/h',
    direction   DOUBLE   DEFAULT 0               COMMENT '方向角',
    order_id    BIGINT   DEFAULT 0               COMMENT '关联订单ID, 0=空闲',
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '上报时间',
    PRIMARY KEY (id),
    INDEX idx_driver_id (driver_id),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='司机实时位置表';
```

### 3.3 push_log — 推送日志表（可选）

```sql
CREATE TABLE IF NOT EXISTS push_log (
    id          BIGINT       NOT NULL AUTO_INCREMENT COMMENT '主键',
    user_id     BIGINT       NOT NULL                COMMENT '用户ID',
    push_type   TINYINT      NOT NULL                COMMENT '类型: 1=App推送 2=短信',
    title       VARCHAR(200) DEFAULT ''              COMMENT '标题',
    content     TEXT         NOT NULL                COMMENT '内容',
    target      VARCHAR(100) DEFAULT ''              COMMENT '目标(设备token/手机号)',
    result      TINYINT      DEFAULT 0               COMMENT '结果: 0=失败 1=成功',
    error_msg   VARCHAR(500) DEFAULT ''              COMMENT '失败原因',
    created_at  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    INDEX idx_user_id (user_id),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='推送日志表';
```

### 3.4 poi — 高德 POI 搜索缓存表（自动创建，无需手动执行）

> 由 `locationsvc` 启动时通过 GORM `AutoMigrate` 自动创建，此处仅作说明。

```sql
CREATE TABLE IF NOT EXISTS poi (
    id         BIGINT        NOT NULL AUTO_INCREMENT  COMMENT '主键',
    name       VARCHAR(100)  NOT NULL                 COMMENT 'POI 名称',
    address    VARCHAR(255)  DEFAULT ''               COMMENT '地址',
    longitude  DECIMAL(10,6) DEFAULT 0                COMMENT '经度',
    latitude   DECIMAL(10,6) DEFAULT 0                COMMENT '纬度',
    category   VARCHAR(100)  DEFAULT ''               COMMENT '分类',
    distance   INT           DEFAULT 0                COMMENT '距搜索中心的距离(米)',
    source     VARCHAR(20)   DEFAULT 'amap'           COMMENT '数据来源',
    created_at DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '缓存写入时间',
    PRIMARY KEY (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='高德POI搜索缓存表';
```

---

## 四、服务端口分配

| 服务 | 端口 | 类型 |
|------|------|------|
| locationsvc | 9001 | RPC |
| pushesvc | 9002 | RPC |
| etcd | 2379 | 注册中心 |
| MySQL | 3306 | 数据库 |
| Redis | 6379 | 缓存 |

---

## 五、启动命令

### 启动中间件

```powershell
# etcd
docker run -d --name etcd -p 2379:2379 quay.io/coreos/etcd:v3.5.11 /usr/local/bin/etcd --listen-client-urls http://0.0.0.0:2379 --advertise-client-urls http://0.0.0.0:2379

# MySQL
docker run -d --name mysql-local -p 3306:3306 -e MYSQL_ROOT_PASSWORD=root mysql:8.0

# Redis
docker run -d --name redis-local -p 6379:6379 redis:7-alpine
```

### 启动 RPC 服务

```powershell
cd D:\gowork\XiaoLong-Ridy

# 终端1：位置服务
go run rpc/locationsvc/locationsvc.go -f rpc/locationsvc/etc/locationsvc.yaml

# 终端2：推送服务
go run rpc/pushesvc/pushesvc.go -f rpc/pushesvc/etc/pushesvc.yaml
```

### Proto 编译命令（新增接口后执行）

```powershell
cd D:\gowork\XiaoLong-Ridy
protoc --go_out=. --go_opt=module=XiaoLong-Ridy --go-grpc_out=. --go-grpc_opt=module=XiaoLong-Ridy --proto_path=. rpc/locationsvc/locationsvc.proto
protoc --go_out=. --go_opt=module=XiaoLong-Ridy --go-grpc_out=. --go-grpc_opt=module=XiaoLong-Ridy --proto_path=. rpc/pushesvc/pushesvc.proto
```

---

## 六、组员使用规范

### 错误处理

```go
import "XiaoLong-Ridy/common/errorx"

// 业务错误
return errorx.NewCodeMsgError(errorx.ErrUserNotFound)

// gRPC 错误转换
return errorx.GrpcError(err)
```

### 返回格式

```go
import "XiaoLong-Ridy/common/response"

// 成功
response.Success(w, data)

// 参数错误
response.ParamError(w, "用户名不能为空")
```

### 常量

```go
import "XiaoLong-Ridy/common/constants"

// 订单状态
status := constants.OrderStatusPending

// Redis Key
key := fmt.Sprintf(constants.RedisKeyDriverLocation, driverId)

// Kafka Topic
topic := constants.TopicLocationReport
```

### ID 生成

```go
import "XiaoLong-Ridy/common/keyutil"

orderId := keyutil.GenerateOrderID()
driverId := keyutil.GenerateDriverID()
userId := keyutil.GenerateUserID()
```

---

## 七、地图服务（locationsvc）接口说明与 API 测试

### 7.1 服务概览

地图服务对外暴露 3 个 gRPC 接口，统一注册在 `locationsvc.LocationService` 下，监听 **9001** 端口。

| 接口 | 功能 | 数据来源 | 状态 |
|------|------|----------|------|
| `ReverseGeocode` | 逆地址解析：经纬度 → 省/市/区/详细地址 | 高德 API（真实） | ✅ 已完成 |
| `POISearch` | POI 搜索：关键词 → 附近地点列表 | 高德 API + 本地 MySQL 缓存 | ✅ 已完成 |
| `RoutePlan` | 路径规划：两点 → 距离/预计时间/路线点串 | 高德 API（真实） | ✅ 已完成 |

### 7.2 接口一：ReverseGeocode（逆地址解析）

**功能**：输入一个经纬度坐标，返回该点所在省份、城市、区县、详细地址和附近 POI 名称。底层调用高德 `/v3/geocode/regeo` 接口。

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `lat` | double | 是 | 纬度 |
| `lng` | double | 是 | 经度 |

**响应字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `province` | string | 省 |
| `city` | string | 市（直辖市时 city 为空数组，自动用省兜底，如"北京市"） |
| `district` | string | 区/县 |
| `address` | string | 详细地址 |
| `poi_name` | string | 附近 POI 名称（base 模式下可能为空） |

**注意**：
- 高德仅覆盖中国境内坐标，**境外坐标会报错**：`无法解析该坐标对应的地址（可能不在高德服务范围内）`。

### 7.3 接口二：POISearch（POI 搜索）

**功能**：以某个坐标为中心、按关键词搜索周边地点，支持半径与分页。底层调用高德 `/v3/place/around` 接口，并带本地缓存。

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `keyword` | string | 是 | 搜索关键词，如"麦当劳" |
| `lat` / `lng` | double | 是 | 中心坐标 |
| `radius` | int32 | 否 | 搜索半径（米），默认按高德规则 |
| `page` | int32 | 否 | 页码 |
| `size` | int32 | 否 | 每页条数 |

**响应字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `items` | repeated POIItem | 地点列表（名称/地址/坐标/分类/距离） |
| `total` | int32 | 结果总数 |

**本地缓存机制**（重点）：

```text
第一次搜索：查本地 poi 表 → 没命中 → 调高德 API → 结果批量写入 poi 表
第二次搜索：查本地 poi 表 → 命中 → 直接返回，不再调高德（省 API 调用）
```

- 缓存表 `poi` 由服务启动时 `AutoMigrate` 自动创建，无需手动建表。
- 命中逻辑为 `name LIKE '%关键词%'`，按距离升序返回前 `size` 条。

### 7.4 接口三：RoutePlan（路径规划）

**功能**：输入起终点坐标，调用高德 `/v3/direction/driving` 接口，返回真实可行驶距离、预计时间和完整路线点串。

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `origin_lat` / `origin_lng` | double | 是 | 起点坐标 |
| `destination_lat` / `destination_lng` | double | 是 | 终点坐标 |

**响应字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `distance` | int32 | 真实驾车距离（米），比直线距离长 |
| `duration` | int32 | 预计行驶时间（秒），结合道路等级估算 |
| `polyline` | string | 完整路线点串 `"lng,lat;lng,lat;..."`，可直接在地图绘制 |
| `origin_lat` / `origin_lng` | double | 起点坐标回显 |
| `dest_lat` / `dest_lng` | double | 终点坐标回显 |

**示例**：深圳科技园 → 深圳北站（`origin: 22.547,113.946`，`dest: 22.609,114.028`）实测返回：`distance=15591m`、`duration=1859s`、`polyline` 约 5000+ 字符。

**注意**：高德路径规划返回的是**驾车路线**，已绕行真实道路（非直线）；若起终点之一在境外或不可驾车区域，会返回高德错误。

### 7.5 高德 API 配置

配置位于 `rpc/locationsvc/etc/locationsvc.yaml`：

```yaml
MapService:
  ApiKey: "8e17f5be48667ed588da48a9822405f0"   # 高德 Web 服务 Key
  Provider: "amap"
  BaseUrl: "https://restapi.amap.com/v3"
```

若 Key 失效，请到 [高德开放平台](https://console.amap.com/) 重新申请。

### 7.6 API 测试使用说明

地图服务是 **gRPC 服务**（非 HTTP），测试方式与普通 REST 接口不同，下面提供三种方式，任选其一。

#### 7.6.1 准备工作

1. 确保中间件已启动：etcd(2379)、MySQL(3306)、Redis(6379)，见"五、启动命令"。
2. 启动位置服务：

```powershell
cd D:\gowork\XiaoLong-Ridy
go run rpc/locationsvc/locationsvc.go -f rpc/locationsvc/etc/locationsvc.yaml
# 看到 "Starting locationsvc rpc server at 0.0.0.0:9001..." 即启动成功
```

> 提示：本服务在 `Mode: dev / test` 下会开启 gRPC reflection，可用 `grpcurl list` 直接列出接口。若配置未写 `Mode`，请在 `locationsvc.yaml` 顶部加一行 `Mode: dev` 后重启。

#### 7.6.2 方式一：grpcurl 命令行（推荐，最快）

grpcurl 是 gRPC 版 curl，安装：

```powershell
go install github.com/fullstorydev/grpcurl/cmd/grpcurl@latest
```

测试接口（注意 Windows PowerShell 里 JSON 的双引号需转义 `\"`）：

**① ReverseGeocode（逆地址解析）**

```powershell
grpcurl -plaintext -d "{\"lat\":22.547, \"lng\":113.946}" 127.0.0.1:9001 locationsvc.LocationService/ReverseGeocode
```

预期返回（深圳南山区）：

```json
{
  "province": "广东省",
  "city": "深圳市",
  "district": "南山区",
  "address": "广东省深圳市南山区科技园...",
  "poiName": "深圳湾科技生态园"
}
```

**② POISearch（POI 搜索）**

```powershell
grpcurl -plaintext -d "{\"keyword\":\"麦当劳\", \"lat\":22.547, \"lng\":113.946, \"radius\":3000, \"size\":10}" 127.0.0.1:9001 locationsvc.LocationService/POISearch
```

预期返回：`items` 数组含多个麦当劳门店，`total` 为总数。再次用相同关键词搜索会命中本地缓存，速度更快。

**③ RoutePlan（路径规划）**

```powershell
grpcurl -plaintext -d "{\"originLat\":22.547, \"originLng\":113.946, \"destinationLat\":22.640, \"destinationLng\":114.035}" 127.0.0.1:9001 locationsvc.LocationService/RoutePlan
```

预期返回：`distance`（真实驾车距离，米）、`duration`（秒）、`polyline`（数千字符的路线点串，非空）。

#### 7.6.3 方式二：Apifox / Apipost / Postman 可视化调试

1. 新建 **gRPC 请求**，服务地址填 `127.0.0.1:9001`。
2. 选择导入 Proto 文件（`D:\gowork\XiaoLong-Ridy\rpc\locationsvc\locationsvc.proto`）或开启 reflection 自动拉取接口列表。
3. 选择 `locationsvc.LocationService` 下的方法，填入请求体（JSON 格式，同上），点发送。

> Postman 需要在请求设置中关闭 TLS 校验（本地为明文端口）。

#### 7.6.4 方式三：临时 Go 测试客户端

在 `tmp/` 目录下写一个临时 main 调用（等价于命令行小工具，便于在 IDE 里断点调试）：

```go
package main

import (
	"context"
	"fmt"

	"XiaoLong-Ridy/rpc/locationsvc/locationsvc"

	"github.com/zeromicro/go-zero/zrpc"
)

func main() {
	cli := zrpc.MustNewClient(zrpc.RpcClientConf{Etcd: zrpc.EtcdConf{Hosts: []string{"127.0.0.1:2379"}, Key: "locationsvc.rpc"}})
	svc := locationsvc.NewLocationServiceClient(cli.Conn())

	resp, err := svc.ReverseGeocode(context.Background(), &locationsvc.ReverseGeocodeReq{Lat: 42.2906, Lng: 123.8442})
	if err != nil {
		fmt.Println("err:", err)
		return
	}
	fmt.Printf("%s %s %s %s\n", resp.Province, resp.City, resp.District, resp.Address)
}
```

运行方式：把文件放到项目内（如 `tmp/regeotest/main.go`），`go run tmp/regeotest/main.go` 即可。

#### 7.6.5 常见问题

| 现象 | 原因与解决办法 |
|------|----------------|
| 报错 `connection refused` | 服务未启动或端口不对，先确认 9001 在监听 |
| 报错 `NotFound` | 未开启 reflection 且未导入 proto 文件，改用 `-proto rpc/locationsvc/locationsvc.proto` 参数 |
| 境外坐标报错"无法解析" | 高德不覆盖境外，属正常现象，换中国境内坐标测试 |
| `POISearch` 第一次很慢、第二次快 | 第一次在调高德并写缓存，属正常现象 |
